services:
  akashic-biomed:
    build: .
    ports:
      - "2236:2236"
    environment:
      NCBI_EMAIL: ${NCBI_EMAIL:-}
      NCBI_API_KEY: ${NCBI_API_KEY:-}
    volumes:
      - ./.akashic-workspace:/workspace
